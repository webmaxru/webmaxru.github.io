{
  "name": "webmaxru-portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "generate:brand": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/generate-brand-assets.ps1",
    "check": "npm run build && node scripts/validate-site.mjs"
  }
}
